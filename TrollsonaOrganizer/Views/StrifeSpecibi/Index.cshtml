@{
  Layout = "_Layout";
}

@using TrollsonaOrganizer.Models;

<h3>Strife Specibi</h3>

@if (@Model.Count == 0)
{
  <h3>No specibi have been created yet!</h3>
} 
else
{
  <ul>
    @foreach (StrifeSpecibus strifeInstance in Model)
    {
      <li>@Html.ActionLink($"{strifeInstance.KindAbstratus}", "Details", new { id = strifeInstance.StrifeSpecibusId })</li>
    }
  </ul>
}

<p>@Html.ActionLink("Create new specibus", "Create")</p>

<p>@Html.ActionLink("Home", "Index", "Home")</p>